// request/response structs + DB models

// ShortUrl struct (id, original_url, short_code, created_at, clicks)

// CreateShortUrlRequest struct (for POST requests)

// ShortUrlResponse struct (for API responses)

// ClickStats struct (for analytics)